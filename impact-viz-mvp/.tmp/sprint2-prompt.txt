You are building the AI-Native Importer for the Benevolence codebase at /home/node/.openclaw/workspace/Benevolence/impact-viz-mvp on branch feature/ai-importer.

Complete Sprint 2 Days 6-10 in order. After each day, commit with the specified message before moving to the next. Do not ask for approval — build, commit, continue.

---

## DAY 6: Validation Rules Engine

### Goal
Replace the basic validation stubs from Sprint 1 with a comprehensive, JSON-configurable rules engine.

### 1. Rewrite lib/import/validator.ts

The existing validator.ts is a stub. Replace it with a full implementation:

```typescript
// Validation rule types
export type RuleName =
  | 'required'
  | 'positive'
  | 'date_valid'
  | 'date_not_future'
  | 'ein_format'
  | 'contribution_type_valid'
  | 'amount_reasonable'
  | 'max_length'
  | 'min_value'
  | 'max_value'
  | 'regex'
  | 'one_of';

export interface ValidationRule {
  rule: RuleName;
  severity: 'error' | 'warning';
  message?: string; // override default message
  // Rule-specific options:
  maxLength?: number;
  minValue?: number;
  maxValue?: number;
  pattern?: string; // for regex rule
  values?: string[]; // for one_of rule
}

export interface ValidationError {
  field: string;
  message: string;
  severity: 'error' | 'warning';
  rule: RuleName;
  value?: unknown; // the actual value that failed
  auto_fixable?: boolean; // hint to AI layer
}

export interface FieldConfig {
  source: string;
  type: 'string' | 'numeric' | 'date' | 'boolean' | 'enum';
  required?: boolean;
  default?: unknown;
  transform?: string;
  confidence?: number;
  values_map?: Record<string, string>;
  rules?: ValidationRule[];
}

export interface EntityMappingConfig {
  source_entity: string;
  field_map: Record<string, FieldConfig>;
  match_criteria?: Array<{ fields: string[]; weight: number }>;
  lookup_rules?: Array<{ by: string; to: string; from: string }>;
}
```

Implement these rules:
- **required**: value is null, undefined, or empty string → error 'Field is required'
- **positive**: numeric value <= 0 → error 'Must be a positive number'
- **date_valid**: value is not a valid date → error 'Invalid date'
- **date_not_future**: date > today + 1 day → warning 'Date is in the future'
- **ein_format**: if value present, must match /^\d{2}-\d{7}$/ after normalization → warning 'EIN format invalid (expected XX-XXXXXXX)', set auto_fixable: true
- **contribution_type_valid**: must be in ['cash','check','wire','stock','crypto','real_estate','other_property'] → error
- **amount_reasonable**: amount_usd > 10_000_000 → warning 'Unusually large amount, please verify'
- **max_length**: string length > maxLength → error
- **min_value**: numeric < minValue → error
- **max_value**: numeric > maxValue → error
- **regex**: value doesn't match pattern → error
- **one_of**: value not in values array → error

Also add default rules per entity type. When validateTransformedRow() is called with entityType, automatically apply these defaults:
- contributions: required=['recipient_name','contribution_date','amount_usd'], contribution_type_valid on contribution_type, positive on amount_usd, date_valid+date_not_future on contribution_date, ein_format on recipient_ein, amount_reasonable on amount_usd
- holdings: required=['name']
- investees: required=['display_name']
- metrics: required=['metric_code','value'], positive on value
- users: required=['matched_existing_user_id OR raw_data.email']

Update the function signature:
```typescript
export function validateTransformedRow(
  transformed: Record<string, unknown>,
  entityType: EntityType,
  entityConfig?: EntityMappingConfig,
  context?: { portfolioId?: string }
): ValidationError[]
```

### 2. Write unit tests: lib/import/validator.test.ts

Cover:
- required rule catches empty string, null, undefined
- positive rule catches 0 and negative
- date_valid catches '2024-13-01', 'not-a-date'
- date_not_future catches tomorrow's date (warning not error)
- ein_format catches '12345678' (8 digits), marks auto_fixable
- contribution_type_valid catches 'PayPal'
- amount_reasonable marks $11M as warning not error
- Full contribution row with no errors passes clean

### 3. Update lib/import/etl-runner.ts

Update runTransformValidate() to use the new validator signature (pass entityType).

Commit message: 'feat: sprint2 day6 — comprehensive validation rules engine with unit tests'

---

## DAY 7: AI Mapping Assistant Service

### Goal
Build the AI service that analyzes CSV samples and suggests field mappings using Claude Haiku.

### 1. Create lib/import/ai/client.ts

Anthropic client wrapper:
```typescript
import Anthropic from '@anthropic-ai/sdk';

const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

export interface AICallOptions {
  model?: string;
  maxTokens?: number;
  temperature?: number;
}

export async function callAI(
  systemPrompt: string,
  userPrompt: string,
  options?: AICallOptions
): Promise<string> {
  const message = await anthropic.messages.create({
    model: options?.model ?? 'claude-haiku-4-5',
    max_tokens: options?.maxTokens ?? 4096,
    temperature: options?.temperature ?? 0.1,
    system: systemPrompt,
    messages: [{ role: 'user', content: userPrompt }],
  });

  const content = message.content[0];
  if (content.type !== 'text') throw new Error('Unexpected response type from AI');
  return content.text;
}

export async function callAIStreaming(
  systemPrompt: string,
  userPrompt: string,
  onChunk: (text: string) => void,
  options?: AICallOptions
): Promise<void> {
  const stream = anthropic.messages.stream({
    model: options?.model ?? 'claude-haiku-4-5',
    max_tokens: options?.maxTokens ?? 4096,
    system: systemPrompt,
    messages: [{ role: 'user', content: userPrompt }],
  });

  for await (const chunk of stream) {
    if (chunk.type === 'content_block_delta' && chunk.delta.type === 'text_delta') {
      onChunk(chunk.delta.text);
    }
  }
}
```

### 2. Create lib/import/ai/prompts/mapping-assist.ts

The mapping assistant prompts (SYSTEM + USER templates):

SYSTEM prompt (exact text):
```
You are an expert data migration specialist for philanthropic software with deep knowledge of Blackbaud Raiser's Edge NXT, Salesforce NPSP, DonorPerfect, and other nonprofit CRM systems.

Your task is to analyze source system field names and sample records, then suggest precise mappings to the Benevolence target schema.

## Benevolence Target Schema

### Entity: investees (Organizations/Nonprofits)
Required: display_name
Optional: ein (format: XX-XXXXXXX), sector, country (default: United States), city, state, impact_theme, description

### Entity: holdings (Investments/Funds)
Required: name
Optional: custodian, asset_class (string), funds_allocated (numeric, USD), as_of (date), cost_basis (numeric), sector, country, description, theory_of_action

### Entity: contributions (tax_contributions)
Required: recipient_name, contribution_date (date YYYY-MM-DD), amount_usd (positive numeric)
Optional: recipient_ein (XX-XXXXXXX), contribution_type (cash|check|wire|stock|crypto|real_estate|other_property), quid_pro_quo_value (numeric default 0), deductible_amount (numeric), acknowledgment_received (boolean), tax_year (int, derived from date if missing)

### Entity: metrics (metric_facts)
Required: metric_code (snake_case string), value (numeric)
Optional: period_start (date), period_end (date), unit (string), source (string)

### Entity: users (portfolio_members/profiles)
Required: email OR display_name
Optional: role (owner|editor|viewer, default: viewer)

## Output Format

Respond ONLY with valid JSON. No markdown, no explanation outside the JSON.

{
  "entity": "investees|holdings|contributions|metrics|users",
  "source_entity": "name of source table/file",
  "confidence_overall": 0.0-1.0,
  "mappings": [
    {
      "target_field": "display_name",
      "source_field": "Constituent Name",
      "confidence": 0.95,
      "reason": "Brief explanation",
      "transform": "normalize_ein|slugify|format_date|null",
      "sample_transforms": [
        {"input": "Smith, John D.", "output": "John D. Smith"}
      ]
    }
  ],
  "unmapped_source_fields": ["Field1", "Field2"],
  "missing_required_targets": ["field_name"],
  "notes": "Any important observations about data quality or edge cases"
}
```

USER prompt template function:
```typescript
export function buildMappingAssistPrompt(params: {
  sourceSystem: string;
  entityType: string;
  sourceFields: string[];
  sampleRecords: Record<string, string>[];
  existingMapping?: Record<string, unknown>;
}): string {
  return `## Source System: ${params.sourceSystem}
## Target Entity: ${params.entityType}
## Source Fields Found: ${params.sourceFields.join(', ')}

## Sample Records (up to 5):
${JSON.stringify(params.sampleRecords.slice(0, 5), null, 2)}

${params.existingMapping ? `## Existing Mapping (improve or confirm):\n${JSON.stringify(params.existingMapping, null, 2)}` : '## No existing mapping — suggest from scratch.'}

Analyze these fields and samples. Suggest precise mappings to the ${params.entityType} target schema.`;
}
```

### 3. Create lib/import/ai/mapping-assist.ts

```typescript
import { callAI } from './client';
import { MAPPING_ASSIST_SYSTEM, buildMappingAssistPrompt } from './prompts/mapping-assist';

export interface MappingSuggestion {
  targetField: string;
  sourceField: string;
  confidence: number;
  reason: string;
  transform?: string;
  sampleTransforms?: Array<{ input: string; output: string }>;
}

export interface MappingAssistResult {
  entity: string;
  sourceEntity: string;
  confidenceOverall: number;
  mappings: MappingSuggestion[];
  unmappedSourceFields: string[];
  missingRequiredTargets: string[];
  notes?: string;
}

export async function suggestMappings(params: {
  sourceSystem: string;
  entityType: string;
  sourceFields: string[];
  sampleRecords: Record<string, string>[];
  existingMapping?: Record<string, unknown>;
}): Promise<MappingAssistResult> {
  const userPrompt = buildMappingAssistPrompt(params);
  const raw = await callAI(MAPPING_ASSIST_SYSTEM, userPrompt, { maxTokens: 2048 });

  // Parse JSON response, with error handling
  try {
    // Strip any accidental markdown code fences
    const cleaned = raw.replace(/^```json\n?/, '').replace(/\n?```$/, '').trim();
    const parsed = JSON.parse(cleaned);
    return {
      entity: parsed.entity,
      sourceEntity: parsed.source_entity,
      confidenceOverall: parsed.confidence_overall,
      mappings: parsed.mappings.map((m: Record<string, unknown>) => ({
        targetField: m.target_field,
        sourceField: m.source_field,
        confidence: m.confidence,
        reason: m.reason,
        transform: m.transform ?? undefined,
        sampleTransforms: m.sample_transforms ?? undefined,
      })),
      unmappedSourceFields: parsed.unmapped_source_fields ?? [],
      missingRequiredTargets: parsed.missing_required_targets ?? [],
      notes: parsed.notes ?? undefined,
    };
  } catch (err) {
    throw new Error(`Failed to parse AI mapping response: ${err}. Raw: ${raw.slice(0, 200)}`);
  }
}
```

### 4. Create app/api/admin/import/mapping-assist/route.ts

```typescript
// POST /api/admin/import/mapping-assist
// Body: { source_system, entity_type, source_fields, sample_records, existing_mapping? }
// Returns: MappingAssistResult
// Admin-only endpoint
import { NextResponse } from 'next/server';
import { suggestMappings } from '@/lib/import/ai/mapping-assist';

export async function POST(req: Request) {
  try {
    const body = await req.json();
    const { source_system, entity_type, source_fields, sample_records, existing_mapping } = body;

    if (!source_system || !entity_type || !source_fields || !sample_records) {
      return NextResponse.json({ error: 'Missing required fields' }, { status: 400 });
    }

    const result = await suggestMappings({
      sourceSystem: source_system,
      entityType: entity_type,
      sourceFields: source_fields,
      sampleRecords: sample_records,
      existingMapping: existing_mapping,
    });

    return NextResponse.json({ data: result });
  } catch (err: unknown) {
    const message = err instanceof Error ? err.message : 'Unknown error';
    console.error('[mapping-assist] Error:', message);
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

### 5. Create lib/import/ai/validate-row.ts

AI validation suggestions for individual rows with errors:

SYSTEM prompt for row validation:
```
You are a data quality specialist for philanthropic software. Analyze a single row that failed validation and suggest intelligent fixes.

Common issues in Blackbaud/nonprofit CRM exports:
- EINs stored as integers, losing leading zeros (123456789 should be 01-2345678)
- Dates in MM/DD/YYYY or YYYYMMDD format instead of YYYY-MM-DD
- ZIP codes truncated (12345 instead of 12345-6789)
- Gift types using non-standard labels (PayPal → cash, Venmo → cash, DAF → other_property)
- Amounts with currency symbols ($1,000.00 → 1000.00)
- Boolean fields as Yes/No strings
- NULL stored as the string 'NULL' or 'N/A'

For each validation error, suggest:
1. auto_fix: corrected value (if you can determine it with >80% confidence)
2. warning: flag for review without blocking
3. skip: row should be skipped with reason
4. manual: requires human judgment

Respond ONLY with valid JSON array. No markdown.

[
  {
    "field": "recipient_ein",
    "suggestion_type": "fix_field",
    "confidence": 0.87,
    "explanation": "EIN stored as integer, losing leading zero. Standard Blackbaud export issue.",
    "proposed_value": "01-2345678",
    "auto_fixable": true,
    "bulk_applicable": true,
    "bulk_condition": "LENGTH(REGEXP_REPLACE(value, '[^0-9]', '', 'g')) = 9"
  }
]
```

Implement suggestRowFixes(params) function returning AISuggestion[].

Commit message: 'feat: sprint2 day7 — AI client, mapping assistant prompts, validate-row AI service'

---

## DAY 8: AI Mapping Suggestions UI

### Goal
Wire AI mapping suggestions into the MappingGrid component so users see confidence scores and AI reasoning.

### 1. Update components/admin/MappingGrid.tsx

The existing MappingGrid shows source→target field dropdowns. Enhance it:

Add props:
```typescript
interface MappingGridProps {
  // ... existing props ...
  aiSuggestions?: MappingAssistResult; // from AI service
  onRequestAISuggestions?: () => void; // trigger AI analysis
  isLoadingAI?: boolean;
}
```

For each row in the grid, if AI has a suggestion for that target field:
- Show confidence badge: green ≥0.85, yellow ≥0.70, red <0.70
- Show AI reasoning in a tooltip or expandable row
- Auto-populate the dropdown with the AI suggestion (but keep editable)
- If no suggestion, show 'No AI match' in gray

Add an 'Ask AI' button at the top of the grid that calls onRequestAISuggestions.

Show a banner when AI suggestions are loaded: 'AI analyzed X fields. Y mapped with high confidence (≥85%), Z need review.'

### 2. Update app/admin/imports/[id]/mapping/MappingPageClient.tsx

Wire in the AI:
- On page load, if CSV files have been uploaded, automatically call POST /api/admin/import/mapping-assist for each entity tab
- Show loading state while AI is running ('AI is analyzing your data...')
- Pass suggestions to MappingGrid
- Show toast on success: 'AI mapped X/Y fields automatically'

### 3. Create components/admin/AIConfidenceBadge.tsx

Reusable confidence badge:
```tsx
// Props: confidence (0-1), showLabel?: boolean
// Renders: colored dot + percentage
// Green: ≥0.85, Yellow: 0.70-0.84, Red: <0.70, Gray: no suggestion
```

### 4. Update components/admin/ImportErrorsTable.tsx

Add AI fix suggestions column:
- For each row with validation errors, show 'AI Fix Available' badge if there are auto_fixable errors
- 'Apply Fix' button that calls POST /api/admin/import/ai/suggest for that row
- Show the suggested fix inline after clicking
- 'Apply All Auto-Fixable' button at top of table

### 5. Create app/api/admin/import/ai/suggest/route.ts

```typescript
// POST /api/admin/import/ai/suggest
// Body: { import_job_id, staging_table, staging_row_ids: string[] }
// For each row: fetch raw_data + validation_errors, call AI, store in import_ai_suggestions, return suggestions
```

Commit message: 'feat: sprint2 day8 — AI suggestions in mapping grid, confidence badges, error fix UI'

---

## DAY 9: Transformer Enrichment

### Goal
Enhance the transformer with EIN→charity lookup and additional data enrichments.

### 1. Update lib/import/transformer.ts

Add enrichment step after field mapping:

```typescript
export interface EnrichmentResult {
  charityId?: string; // if EIN matched a charity
  charityName?: string;
  charityData?: Record<string, unknown>;
  geocoded?: { lat: number; lon: number; city?: string; state?: string };
}

export async function enrichTransformedRow(
  supabase: SupabaseClient,
  entityType: EntityType,
  transformed: Record<string, unknown>
): Promise<EnrichmentResult>
```

Enrichments:
- **EIN → Charity lookup**: if entity has ein/recipient_ein, query charities table by ein. Store charity_id if found.
- **Name → Charity fuzzy match**: if no EIN but has recipient_name, query charities using full-text search (charities.search_vector). Return top match if confidence > 0.8 (based on text similarity).
- **Tax year derivation**: if contribution has contribution_date but no tax_year, derive: tax_year = year of contribution_date (or year+1 if date is Jan-Apr — configurable).
- **Deductible amount calculation**: if deductible_amount is null, compute: amount_usd - (quid_pro_quo_value ?? 0).
- **AGI limit category**: if recipient_type present, call determineAGILimitCategory() from lib/tax/agi-calculator.ts.

Update runTransformValidate() in etl-runner.ts to call enrichTransformedRow() for each row and store results in transformed_data.

### 2. Update staging_import_contributions and staging_import_investees

Add enrichment metadata to transformed_data JSONB structure:
```json
{
  "...field mappings...",
  "_enrichment": {
    "charity_id": "uuid-or-null",
    "charity_match_confidence": 0.95,
    "charity_match_method": "ein_exact|name_fuzzy|none",
    "tax_year_derived": true,
    "deductible_amount_derived": true
  }
}
```

### 3. Add EIN normalization utility: lib/import/utils/normalize-ein.ts

```typescript
// Takes various EIN formats and normalizes to XX-XXXXXXX or null
// Handles: '123456789' → '12-3456789'
//          '12-3456789' → '12-3456789' (already valid)
//          '0123456789' (10 digits) → try stripping leading 0 → '12-3456789'
//          '12345678' (8 digits) → pad with leading 0 → '01-2345678'
//          'N/A', null, '' → null
export function normalizeEIN(raw: string | null | undefined): string | null
```

### 4. Create lib/import/utils/date-parser.ts

Robust date parsing for messy import data:
```typescript
// Tries multiple formats in order:
// YYYY-MM-DD, MM/DD/YYYY, M/D/YYYY, MM-DD-YYYY, YYYYMMDD, 'January 1, 2024', Unix timestamp
// Returns ISO string or null if unparseable
export function parseFlexibleDate(raw: string | null | undefined): string | null

// Derive tax year from a contribution date
// Logic: Jan-Mar contributions typically apply to prior tax year (configurable)
export function deriveTaxYear(contributionDate: string, priorYearMonths?: number[]): number
```

### 5. Update transformer to use new utilities

Replace any inline date/EIN handling in transformer.ts with the new utility functions.

Commit message: 'feat: sprint2 day9 — enrichment (EIN→charity lookup, tax year, deductible amount), date/EIN utils'

---

## DAY 10: Progress Monitor + Error Browser Polish

### Goal
Real-time progress updates and polished error browsing experience.

### 1. Create lib/import/progress-emitter.ts

Server-Sent Events (SSE) helper for streaming progress:
```typescript
// Manages SSE connections per import job
// When ETL worker updates progress, emit to any listening clients

export class ImportProgressEmitter {
  private static clients = new Map<string, Set<ReadableStreamDefaultController>>();

  static subscribe(importJobId: string): ReadableStream {
    // Returns an SSE stream for this job
    // Sends heartbeat every 15s to prevent timeout
  }

  static emit(importJobId: string, event: ProgressEvent): void {
    // Broadcast to all clients subscribed to this job
  }

  static unsubscribe(importJobId: string, controller: ReadableStreamDefaultController): void {
    // Clean up when client disconnects
  }
}

export interface ProgressEvent {
  type: 'extraction' | 'validation' | 'loading' | 'complete' | 'error' | 'heartbeat';
  entity?: string;
  processed?: number;
  total?: number;
  percent?: number;
  message?: string;
  etaSeconds?: number;
}
```

### 2. Create app/api/admin/imports/[id]/progress/route.ts

```typescript
// GET /api/admin/imports/:id/progress
// Returns SSE stream of ProgressEvent objects
// Content-Type: text/event-stream
export async function GET(req: Request, { params }: { params: { id: string } }) {
  const stream = ImportProgressEmitter.subscribe(params.id);
  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
    },
  });
}
```

### 3. Update ETL runner to emit progress

In lib/import/etl-runner.ts, after each batch of 200 rows:
```typescript
ImportProgressEmitter.emit(importJobId, {
  type: 'validation',
  entity: entityType,
  processed: totalProcessed,
  total: totalRows,
  percent: Math.round((totalProcessed / totalRows) * 100),
  etaSeconds: estimatedRemaining,
});
```

### 4. Create components/admin/ImportProgressMonitor.tsx (Client Component)

Real-time progress display:
```tsx
// Props: importJobId, initialJob (from server)
// Connects to SSE endpoint: /api/admin/imports/:id/progress
// Falls back to polling GET /api/admin/imports/:id every 5s if SSE fails

// Displays:
// - 4 stat cards: Extracted / Validated / Loaded / Failed
// - Progress bars per entity type (holdings, investees, contributions, metrics)
// - Current phase label (Extracting... / Validating... / Loading... / Complete)
// - ETA display when available
// - Live error count with link to error browser
// - Action buttons: Pause (if running), Resume (if paused), Rollback, Commit

// When status = 'paused': show banner 'Ready for review. X errors found. Review before loading.'
// When status = 'completed': show success banner with summary stats
// When status = 'failed': show error banner with message
```

### 5. Update app/admin/imports/[id]/page.tsx

Replace static progress cards with ImportProgressMonitor component.

### 6. Polish ImportErrorsTable.tsx

- Add 'Show only errors' / 'Show all' toggle
- Add entity type filter tabs (All | Holdings | Investees | Contributions | Metrics)
- Add severity filter (Errors only | Warnings only | All)
- Highlight auto-fixable rows with a special indicator
- 'Export to CSV' button: download failed rows as CSV for manual editing
- Keyboard navigation: arrow keys to move between rows

### 7. Sprint 2 Wrap-Up
- Run: pnpm tsc --noEmit 2>&1 | head -40
- Fix critical TypeScript errors
- Run: pnpm test --run 2>&1 | tail -20
- Update docs/AI_IMPORTER_BLUEPRINT.md Sprint 2 section as DONE
- Commit all remaining changes

Commit message: 'feat: sprint2 day10 — SSE progress monitor, polished error browser, sprint2 complete'

---

## Final Instructions

- Complete each day fully before moving to the next
- Fix errors and continue — do not stop
- Use TypeScript, avoid 'any' where possible
- Follow existing code patterns (check lib/ and components/ for conventions)
- After ALL 5 days: run pnpm tsc --noEmit, fix critical errors
- Update SPRINT_PROGRESS.md: mark Days 6-10 as Done
- Report: days completed, files created, total commits, any issues